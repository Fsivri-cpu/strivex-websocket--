{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "nix": {
      "packages": ["python3Full", "pkgs.tcl", "pkgs.tk"]
    }
  },
  "deploy": {
    "startCommand": "uvicorn chat_service:app --host 0.0.0.0 --port $PORT"
  }
}
